<UserControl x:Class="LightScape.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightScape"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border
            Background="#55252135"
            CornerRadius="16" 
            Padding="12" 
            Margin="24,54,24,24"
            BorderBrush="#88FFCB77"
            BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel HorizontalAlignment="Left">

                    <TextBlock Text="Settings"
                               Foreground="White"
                               FontSize="24"
                               Margin="0,0,0,12"/>


                    <TextBlock Text="Ed Easteregg"
                               Foreground="White"
                               FontSize="16"
                               Margin="0,0,0,12"/>

                    <!--Togglebutton um das Ed Easteregg zu aktivieren oder zu deaktivieren -->
                    <ToggleButton x:Name="EdToggleButton"
                                  Margin="0"
                                  Content="Ed-Mode On / Ed-Mode Off"
                                  Width="160"
                                  Checked="Toggle_EdMode_Checked"
                                  Unchecked="Toggle_EdMode_Unchecked"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
